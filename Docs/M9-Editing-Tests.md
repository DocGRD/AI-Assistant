# M9 — Context-Aware Editing (Propose/Commit) Tests

**Date tested:** _______________
**Tester:** _______________
**Branch:** _______________

Proves Milestone 9: the assistant only ever **proposes** an edit; the plugin dialog is the single
commit point; nothing is overwritten autonomously. See [[User-Guide]] §6.5 for usage.

> **Deterministic** tests run offline (no API quota). **Live** tests use the running service +
> Obsidian. Run Python from the repo root with the venv active; on Windows prefix with
> `PYTHONIOENCODING=utf-8`. Rebuild + redeploy the plugin (`npm run build` in `obsidian-plugin/`,
> copy `main.js`/`styles.css` to `.obsidian/plugins/ai-assistant/`) and reload it in Obsidian.

---

## Section 1 — Automated (deterministic, no network)

### T9.01 — Edit + context unit tests pass
**Steps:** `PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m unittest tests.test_chat_context tests.test_editing -v`

**Expected:** All pass. Covers active-note + selection injection; edit returns a proposal
(fence-stripped, offsets echoed, `allow_webui=False`); word options parsed/deduped; edit without a
selection → 400; exhaustion → `edit_handoff_required`; watcher staging (body untouched, anchor,
status); bad heading → error; private threaded; staged proposal commits in a round-trip.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

## Section 2 — Live editing (plugin)

### T9.02 — Attach a selection
**Steps:** Select text in a note → click **+ Selection** in the sidebar.

**Expected:** A "📎 N chars (scope)" badge appears; an **✎ Edit** toggle shows next to it.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.03 — Propose a paragraph edit (toggle) and Replace
**Steps:** Select a sentence, **+ Selection**, turn on **✎ Edit**, send "fix grammar". Click **Replace**.

**Expected:** A proposal with an original → proposed diff; Replace updates exactly that range, nothing
else in the note changes.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.04 — `/edit` prefix works without the toggle
**Steps:** Select text, **+ Selection**, send `/edit make this more concise`.

**Expected:** Same proposal flow (the `/edit ` prefix triggers edit mode).

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.05 — Word edit shows option chips
**Steps:** Select a single word, **+ Selection**, **✎ Edit**, send "synonyms".

**Expected:** Several **option chips**; clicking one replaces just that word at its offset.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.06 — Keep editing refines the same region
**Steps:** On a proposal, click **Keep editing**, change the instruction, send again.

**Expected:** The selection is restored; a new proposal targets the same region.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.07 — Drift guard
**Steps:** Propose an edit, then (before Replace) edit that region manually in the note. Click **Replace**.

**Expected:** It refuses with "the note changed … re-select the region" — no wrong overwrite.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.08 — Whole-note edit preserves frontmatter
**Steps:** Select the whole body (not the YAML), **+ Selection**, **✎ Edit**, send "rewrite clearly", Replace.

**Expected:** The body is replaced; the YAML frontmatter is untouched.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.09 — Private edit never uses Google
**Steps:** Turn on **🔒 Private** (or edit a note with `private: true`), propose an edit, watch `verbose`/logs.

**Expected:** The edit is generated by a `trains_on_data=no` provider (Groq/Cerebras), never Google.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.10 — WebUI edit opt-in
**Steps:** Force exhaustion (e.g. private with no no-train key, or all providers down) on an edit.

**Expected:** A "Web Edit Handoff" prompt to copy; pasting the web AI's revised text produces a normal
proposal you can **Replace**. No silent handoff.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

## Section 3 — Vault staging (watcher) + plugin commit

### T9.11 — Section edit stages a proposal (body untouched)
**Steps:** Add `assistant-status: pending`, `assistant-request`, `assistant-edit: true`,
`assistant-edit-scope: section`, `assistant-edit-target: <heading>`; save; let the watcher run.

**Expected:** Console shows "📝 Edit proposal staged"; the note gains an `## Assistant Proposed Edit`
block and `assistant-status: proposal-pending`; **your section text is unchanged**.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.12 — Plugin commits the staged proposal
**Steps:** Open that note with the plugin running. In the sidebar proposal, click **Replace**.

**Expected:** The section is replaced; the proposal block is removed; `assistant-status` becomes
`done`. Nothing else changes.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.13 — Bad heading → error, not a proposal
**Steps:** Stage a `section` edit with `assistant-edit-target:` a heading that doesn't exist.

**Expected:** The note gets an error section; **no** `AI-EDIT-PROPOSAL` block; body untouched.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.14 — Anchor miss → "select manually"
**Steps:** Stage a proposal, then change the targeted text in the note before committing in the plugin.

**Expected:** Replace reports "couldn't locate the region — select manually"; no wrong overwrite.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.15 — Whole-note vault edit
**Steps:** Stage with `assistant-edit-scope: whole-note` (no target).

**Expected:** Proposal staged against the whole body; commit replaces the body, preserves frontmatter.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T9.16 — Private vault edit
**Steps:** Stage an edit on a note with `private: true`.

**Expected:** Generated only by `trains_on_data=no` providers; if exhausted, an error explains the
options (it never silently uses a training provider or a web AI).

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

## Section Summary

| Tests | Pass | Fail | Skip |
|-------|------|------|------|
| 16 | | | |

**Deterministic:** T9.01 (the rest are Live/Obsidian).

**Overall M9 result:** _______________

**Open bugs:**

_______________
