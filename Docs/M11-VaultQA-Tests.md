# M11 — Vault QA (zero-cost local RAG) Tests

**Date tested:** _______________
**Tester:** _______________
**Branch:** _______________

Proves Milestone 11: semantic search + grounded, cited answers over the whole vault, with local
embeddings (free + private). Indexing runs on the always-on box, not the laptop. See [[User-Guide]]
§6.6–6.7.

> **Deterministic** tests run offline with a fake embedder (no model download). **Live** tests run on
> the **indexing box** (and the plugin). Run Python from the repo root with the venv active; on
> Windows prefix `PYTHONIOENCODING=utf-8`.

---

## Section 1 — Automated (deterministic, no network)

### T11.01 — RAG unit tests pass
**Steps:** `PYTHONIOENCODING=utf-8 .venv/Scripts/python.exe -m unittest tests.test_rag tests.test_chat_context -v`

**Expected:** All pass — chunker, vector store (search/save-load/model-change), incremental indexer,
retriever ranking + cited context, `run_vault_qa` (public source stays non-private; any private source
forces private), RagService (enabled flag, excludes, change-only re-index), and the server Vault-QA
path (answer + sources; no-index message). Plus the API-token auth tests.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

## Section 2 — Indexing (on the box)

### T11.02 — Build the index
**Steps:** On the box: `python assistant.py --terminal` → `vault:reindex`.

**Expected:** First run downloads the model once, then reports `{added, updated, removed, skipped,
chunks}` and an index holding ~your note count. `data/vault_index/` now exists (outside the vault).

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T11.03 — Incremental reindex skips unchanged notes
**Steps:** Run `vault:reindex` again without editing anything.

**Expected:** Report shows `added=0, updated=0`, most notes `skipped` (only changed notes re-embed).

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T11.04 — Startup index + watcher upkeep
**Steps:** With `index_on_startup: true`, restart the service; then edit and save one note.

**Expected:** Startup prints "Index ready: …"; editing a note logs `[RAG] Re-indexed changed note: …`
for that note only.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T11.05 — Laptop does not index
**Steps:** On the laptop (`index_on_startup` false / unset), start the service.

**Expected:** Startup says indexing is OFF on this machine; the watcher does not re-embed; CPU stays idle.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

## Section 3 — Asking

### T11.06 — `vault:ask` answers from across notes (terminal)
**Steps:** On the box: `vault:ask <a question whose answer spans multiple notes>`.

**Expected:** A grounded answer, then a `Sources:` line listing the real notes used.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T11.07 — Plugin Vault QA toggle + source chips
**Steps:** In the sidebar, turn on **📚 Vault QA**, ask a cross-note question.

**Expected:** A cited answer with clickable **source chips**; clicking one opens that note.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T11.08 — No-index message
**Steps:** Ask Vault QA against a service with no index (e.g. the laptop).

**Expected:** A clear "No vault index … run vault:reindex on the indexing host" message — no error.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T11.09 — Grounded, not hallucinated
**Steps:** Ask Vault QA something that is clearly NOT in your vault.

**Expected:** It says it can't find the answer in your notes (rather than inventing one).

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

## Section 4 — Privacy

### T11.10 — Private source forces no-train routing
**Steps:** Ask a Vault QA question whose best answer lives in a note flagged `private: true`; watch
`verbose`/logs.

**Expected:** The answer is generated by a `trains_on_data=no` provider (Groq/Cerebras), never Google,
and is never sent to a web AI.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

## Section 5 — LAN access

### T11.11 — API token enforced
**Steps:** On the box set `host: 0.0.0.0` + `api_token`; from another device `curl http://<box>:<port>/status`
without and with `-H "X-API-Key: <token>"`.

**Expected:** 401 without the header; 200 with it. Startup printed the LAN URL + "X-API-Key required".

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

### T11.12 — Laptop plugin uses the box
**Steps:** In the laptop plugin set Host = box IP, Port, and the API token; use Live mode (chat / Vault QA).

**Expected:** The sidebar shows "Connected"; chat and Vault QA work against the box's providers + index.

- [ ] ✅ Pass  &nbsp; [ ] ❌ Fail  &nbsp; [ ] ⏭ Skip

**Notes:**
_______________

---

## Section Summary

| Tests | Pass | Fail | Skip |
|-------|------|------|------|
| 12 | | | |

**Deterministic:** T11.01 (the rest are Live: box index, plugin, LAN).

**Overall M11 result:** _______________

**Open bugs:**

_______________
